apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "with_ance.fullname" . }}-test-connection"
  labels:
    {{- include "with_ance.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "with_ance.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
